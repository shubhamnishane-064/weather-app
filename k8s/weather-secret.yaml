apiVersion: v1
kind: Secret
metadata:
  name: weather-secret
type: Opaque
data:
  # Replace "your-api-key-here" with your actual API key.
  # IMPORTANT: The value MUST be base64 encoded.
  # To encode: echo -n "your-api-key-here" | base64
  api-key: your-api-key-here
